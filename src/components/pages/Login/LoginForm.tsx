import { TextField } from "@mui/material";

const LoginForm = () => {
  return (
    <TextField
      id="phoneNumber"
      label="Phone Number"
      variant="standard"
      fullWidth
    />
  );
};

export default LoginForm;
